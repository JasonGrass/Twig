<Window
    x:Class="Twig.Wpf.Example.Views.MainWindowView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Twig.Wpf.Example"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Twig.Wpf.Example.ViewModels"
    xmlns:views="clr-namespace:Twig.Wpf.Example.Views"
    Title="MainWindowView"
    Width="800"
    Height="450"
    d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <views:FirstSubView
            Grid.Column="0" Margin="4" Background="Gray">
        </views:FirstSubView>

        <views:FirstSubView
            Grid.Column="1" Margin="4" Background="DarkSeaGreen">
        </views:FirstSubView>

    </Grid>
</Window>
